# Poptart redesign dossier

The design grill is complete and the shared product/architecture understanding is confirmed.

- `SPEC.md` is the consolidated build specification and acceptance contract.
- `PRODUCT-SPEC.md` is the ticket-ready product specification produced from the confirmed dossier.
- `CONTEXT.md` defines the domain language.
- `adr/` records the individual confirmed decisions.
- `poptart-runtime.drawio` is the editable runtime architecture diagram.
- `FOLLOW-UPS.md` contains deliberately excluded post-MVP candidates.

These files are provisional only in location. Before implementation, move them into the clean native Swift repository so decisions evolve beside the code. The legacy repository remains unchanged by this dossier.

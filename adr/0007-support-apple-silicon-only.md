# Support Apple Silicon only

The redesigned Poptart will support Apple Silicon Macs only, with M1 as the minimum hardware baseline. This makes local inference performance, unified memory, Metal acceleration, packaging, and the 1.5-second Completion Deadline first-class assumptions rather than optional fast paths alongside an Intel fallback.

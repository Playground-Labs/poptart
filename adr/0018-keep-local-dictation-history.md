# Keep local Dictation history

The MVP will keep a local history of Dictation Records containing the Raw Transcript, the text actually inserted, whether Cleanup changed it, any fallback reason, and relevant stage timings. Nearby application context is ephemeral and may not be retained in history.

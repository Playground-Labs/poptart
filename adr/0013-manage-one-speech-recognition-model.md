# Manage one speech-recognition model

The Poptart MVP will select, install, configure, and update one English speech-recognition model rather than exposing a model catalog or selector. Model identity remains hidden behind the recognition seam so Poptart can improve or replace the implementation while preserving one stable Dictation experience and latency contract.

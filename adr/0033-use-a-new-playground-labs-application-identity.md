# Use a new Playground Labs application identity

The redesigned Poptart will use a new Playground Labs-owned bundle identifier and install alongside the legacy Poptart during beta. It will receive its own permissions, Keychain items, application data, updater state, and release channel, preventing accidental coupling to the inherited application while the existing version remains available as a fallback.

# Give the redesign the canonical repository name

The new native Swift repository will become the canonical `Playground-Labs/poptart` project. The inherited repository will be renamed and archived as `poptart-legacy`, making project identity reflect the redesigned architecture rather than preserving a temporary `next` suffix indefinitely; repository changes wait until the design interview is complete.

# Build macOS-first in native Swift

The redesigned Poptart will initially target macOS and be implemented as a fully native Swift application. Native integration, product quality, and rapid iteration on Apple Silicon take priority over preserving the legacy cross-platform architecture; Windows and Linux are deferred rather than treated as constraints on the first architecture.

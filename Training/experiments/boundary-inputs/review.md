Both independent specification and standards reviews cleared all64 training-only labels. Corrected subject-type/terminal-punctuation confound within each filler; both casing/punctuation and subject/punctuation combinations are balanced. Native/Python contract checks run before training. Validation/test unchanged; frozen release holdout unqueried. Synthetic coverage does not establish generalization.
